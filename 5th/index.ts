let famous_Person: string = "Andrew Tate";
const quote:string="Arrogance breeds complacency and complacency breeds failure."

const message = `${famous_Person} once said "${quote}`;
console.log(message);