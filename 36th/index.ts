function make_shirt(size: string, message: string): void {
    console.log(`The shirt is size ${size} and the message printed on it reads "${message}".`);
  }
  
  make_shirt("large", "I love TypeScript");

  